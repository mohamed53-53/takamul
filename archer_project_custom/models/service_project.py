from odoo import models, fields, api, _

# class ProjectService(models.Model):
#     _name = "project.service"
#     _rec_name = 'product_id'
#
#     product_id = fields.Many2one(string='product.template', domain=[('provide_service','=',True)])
